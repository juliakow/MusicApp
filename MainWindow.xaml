<Window x:Class="MusicPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MusicPlayer"
        Title="Music Player" Height="500" Width="800"
        Background="#FF1E1E1E">

	<Window.Resources>
		<local:PathToFileNameConverter x:Key="PathToFileNameConverter"/>

		<Style TargetType="Button">
			<Setter Property="Background" Value="#FF3A3A3A"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="BorderBrush" Value="#FF555555"/>
			<Setter Property="Margin" Value="5"/>
			<Setter Property="FontSize" Value="14"/>
			<Setter Property="HorizontalContentAlignment" Value="Center"/>
			<Setter Property="VerticalContentAlignment" Value="Center"/>
			<Setter Property="MinWidth" Value="40"/>
			<Setter Property="MinHeight" Value="40"/>
		</Style>

		<Style TargetType="Slider">
			<Setter Property="Background" Value="#FF3A3A3A"/>
			<Setter Property="Foreground" Value="#FF1E90FF"/>
		</Style>
	</Window.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		
		<StackPanel Grid.Row="0" Margin="10">
			<TextBlock x:Name="SongTitle" Text="Brak utworu" Foreground="White" FontSize="18" FontWeight="Bold"/>
			<TextBlock x:Name="SongInfo" Text="Bitrate: - | Sample rate: -" Foreground="LightGray" Margin="0,5"/>
			<TextBlock x:Name="CurrentTime" Text="00:00 / 00:00" Foreground="White"/>
		</StackPanel>

		
		<ListBox x:Name="Playlist" Grid.Row="1" Margin="10,0,10,10"
                 Background="#FF252526" Foreground="White"
                 SelectionChanged="Playlist_SelectionChanged">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<TextBlock Text="{Binding Converter={StaticResource PathToFileNameConverter}}"
                               Margin="5" TextTrimming="CharacterEllipsis"/>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>

	
		<Border Grid.Row="2" Margin="10" Background="#FF252526" Padding="10">
			<StackPanel>
				<ProgressBar x:Name="ProgressBar" Height="10" Margin="0,0,0,10"/>

				<StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
					<Button x:Name="ShuffleButton" Content="🔀" Click="ShuffleButton_Click"
							ToolTip="Losowe odtwarzanie"/>
					<Button x:Name="PreviousButton" Content="⏮" Click="PreviousButton_Click"
							ToolTip="Poprzedni utwór"/>
					<Button x:Name="PlayButton" Content="▶" Click="PlayButton_Click"
							ToolTip="Odtwarzaj"/>
					<Button x:Name="PauseButton" Content="⏸" Click="PauseButton_Click"
							ToolTip="Pauza"/>
					<Button x:Name="NextButton" Content="⏭" Click="NextButton_Click"
							ToolTip="Następny utwór"/>
					<Button x:Name="RepeatButton" Content="🔁" Click="RepeatButton_Click"
							ToolTip="Zapętlanie"/>
				</StackPanel>
			</StackPanel>
		</Border>
	</Grid>
</Window>